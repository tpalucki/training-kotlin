package io.tpalucki

// Recursive DFS on Graph
// Time: O(V + E) | Space: O(V) stack depth
fun dfs(
    node: Int,
    adjList: Map<Int, List<Int>>,
    visited: HashSet<Int> = HashSet(),
) {
    // visit
}
